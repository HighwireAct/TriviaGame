class Question {
    constructor(statement, choices, answer, image) {
        this.statement = statement;
        this.choices = choices;
        this.answer = answer;
        this.image = image;
    }
}